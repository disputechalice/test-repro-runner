name: Check no-subscription
on:
  workflow_dispatch:

jobs:
  no-subscription:
    uses: ./.github/workflows/reusable/check-plain.yaml
    with:
      git-url: https://github.com/free-pmx/free-pmx-no-subscription.git
      git-ref: main
