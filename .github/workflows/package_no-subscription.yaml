name: Check package - no-subscription
on:
  workflow_dispatch:

jobs:
  no-subscription:
    uses: ./.github/workflows/_check-plain.yaml
    with:
      git-repo: https://github.com/free-pmx/free-pmx-no-subscription.git
      git-ref: main
